# Python - Network #0

This project is part of the **Holberton School** curriculum. It focuses on the basics of networking using Bash scripts with `curl` and Python with the `requests` package. The goal is to understand how HTTP requests work, handle responses, and manage status codes and headers.
